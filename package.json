{
  "name": "opensearch-migrate-job",
  "version": "1.0.0",
  "description": "Migrate data from pageseeker_response to pageseeker_response_opensearch with transform + upsert",
  "main": "migrate_records.js",
  "scripts": {
    "start": "node migrate_records.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "dotenv": "^17.2.3"
  }
}
